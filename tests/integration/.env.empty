# Intentionally empty — used by tests/integration/global-setup.ts to
# isolate `nuxt generate` from the developer's local `.env`. Without
# this, Nuxt's auto-dotenv loader bakes the real GA4 measurement ID
# into the integration build and breaks the inert-path tests.
