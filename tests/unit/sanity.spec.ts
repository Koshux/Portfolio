import { describe, it, expect } from 'vitest'

// Placeholder unit test — replace with real specs as composables/utils are added.
describe('sanity', () => {
  it('runs the unit suite', () => {
    expect(1 + 1).toBe(2)
  })
})
